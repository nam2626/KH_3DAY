export default () => {
  return (
    <div>
      <h2>로그인</h2>
      <input type="text" placeholder="아이디 입력" />
      <input type="password" placeholder="암호 입력" />
      <button>로그인</button>
    </div>
  );
};
